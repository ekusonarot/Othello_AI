#include <fstream>
#include <iostream>
/*
int main(){
    std::ofstream file("a", std::ios::binary);
    file << 2.5 ;
    file << 4.2 << ' ';
    file.close();

    std::ifstream file1("a", std::ios::binary);
    double a,b;
    file1 >> a;
    file1 >> b;
    std::cout << a << std::endl;
    std::cout << b << std::endl;
    file1.close();
}
*/

